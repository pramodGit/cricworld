server {
    server_name lavanistore.in;

    root /var/www/lavanistore/frontend/dist;
    index index.html;
	
    location /cdn/images/ {
      alias /var/www/lavanistore/storage/images/;

      expires 30d;
      add_header Cache-Control "public, max-age=2592000";
    }

    location / {
        try_files $uri /index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/lavanistore.in/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/lavanistore.in/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = lavanistore.in) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name lavanistore.in;
    return 404; # managed by Certbot


}

# ================================
# GLOBAL (must be outside server)
# ================================
upstream backend_cluster {
    server localhost:5000;
    server localhost:5001;
    server localhost:5002;
}

upstream websocket_cluster {
    server 127.0.0.1:6000;
    server 127.0.0.1:6001;
    server 127.0.0.1:6002;
}
# ================================
# cricworld.pramod.click - viewer
# ================================
server {
    server_name cricworld.pramod.click;

    root /opt/app/cricworld/viewer-ui/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # 🔥 Load balanced API
    location /api/ {
        proxy_pass http://backend_cluster;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    # Load balanced WebSocket
    location /ws {
        proxy_pass http://websocket_cluster;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;

        # 🔥 FIXES DISCONNECT
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;

        proxy_buffering off;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/cricworld.pramod.click/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/cricworld.pramod.click/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

# ================================
# scorer.cricworld.pramod.click
# ================================
server {
    server_name scorer.cricworld.pramod.click;

    root /opt/app/cricworld/scorer-ui/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/scorer.cricworld.pramod.click/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/scorer.cricworld.pramod.click/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

server {
    if ($host = cricworld.pramod.click) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name cricworld.pramod.click;
    return 404; # managed by Certbot


}
server {
    if ($host = scorer.cricworld.pramod.click) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name scorer.cricworld.pramod.click;
    return 404; # managed by Certbot


}